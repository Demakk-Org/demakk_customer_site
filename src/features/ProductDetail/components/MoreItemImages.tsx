import { Box } from '@mui/material';
import React from 'react';

export default function MoreItemImages({ data }: any) {
  return (
    <div>
      <Box display={{ xs: 'none', sm: 'flex' }}>related images</Box>
    </div>
  );
}
